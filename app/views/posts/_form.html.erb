<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :pic_id %><br>
      <%= f.select :pic_id, options_from_collection_for_select(@pics, :id, :to_s, selected: @post.pic_id) %>
    </div>
    <div class="field">
      <%= f.label :title %><br>
      <%= f.number_field :title, class: "form-control" %>
    </div>
   <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text, class: "form-control", rows:"3" %>
   </div>

  <div class="actions">
    <%= f.submit class:"btn btn-success" %>
  </div>
<% end %>